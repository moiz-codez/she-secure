name: she_secure
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  provider: ^6.1.5
  google_fonts: ^6.3.2
  firebase_core: ^4.14.0
  firebase_auth: ^6.6.1
  cloud_firestore: ^6.9.0
  flutter_contacts: ^2.3.1
  geolocator: ^14.0.3
  maplibre_gl: ^0.27.0
  permission_handler: ^13.0.1
  url_launcher: ^6.3.2
  another_telephony: ^0.4.1
  flutter_background_service: ^5.1.0
  flutter_local_notifications: ^22.3.0
  record: ^7.1.1
  tflite_flutter: ^0.12.1
  camera: ^0.12.0+2
  path_provider: ^2.1.6
  share_plus: ^13.3.0
  gal: ^2.3.3
  flutter_ringtone_player: ^4.0.0+4
  image_picker: ^1.2.3


# permission_handler_android 14.0.0 requires Android SDK Platform 37, whose
# auto-installed package on this machine doesn't match the "android-37"
# target hash Gradle looks for (a preview/point-release SDK naming quirk).
# Pinned to the prior stable release, which targets the already-working
# compileSdk 36 — remove this override once SDK Platform 37 installs cleanly
# or a permission_handler_android release fixes the mismatch.
dependency_overrides:
  permission_handler_android: 13.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

# Regenerates android/app/src/main/res/mipmap-*/ic_launcher.png (and the
# adaptive-icon layers) from assets/images/logo_icon.png. Run with:
#   dart run flutter_launcher_icons
# whenever the logo changes — this config doesn't run automatically.
#
# Uses logo_icon.png (a padded copy of logo.png), not logo.png directly —
# the shield fills logo.png's canvas almost edge-to-edge vertically but has
# real margin on the sides, so an adaptive icon's circular/squircle mask
# clips the top point and bottom curve before it ever touches the sides.
# logo_icon.png adds even margin on all four sides so the whole shield
# fits inside the safe zone on every launcher shape. The in-app logo
# (AppLogo widget) keeps using plain logo.png — it just does BoxFit.contain
# in a box, no masking, so it never had this problem.
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/images/logo_icon.png"
  min_sdk_android: 21
  # This logo has real transparency (unlike the earlier .jpg), so the
  # background behind it can match the app's own dark theme instead of
  # needing a white badge for contrast.
  adaptive_icon_background: "#161826"
  adaptive_icon_foreground: "assets/images/logo_icon.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # YAMNet: on-device audio classifier for Distress Listening (521 AudioSet
  # classes, includes "Screaming" and "Crying, sobbing"). Bundled from
  # Google's published MediaPipe/TF-Hub release, not generated by this repo.
  assets:
    - assets/models/yamnet.tflite
    - assets/models/yamnet_class_map.csv
    - assets/images/logo.png

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
